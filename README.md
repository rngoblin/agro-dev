# Демо-кейс B2B-лендинга для жидких микроудобрений

Обезличенный демонстрационный лендинг-кейс для портфолио веб-дизайнера/фронтенд-разработчика.

Проект показывает, как можно переупаковать сложный производственный B2B-продукт в современный лендинг: ясный первый экран, структурированная продуктовая линейка, понятный путь к заявке, адаптивная сетка и спокойная визуальная система.

Важно: это демо-кейс для портфолио, а не официальный сайт компании.

## Стек

- Next.js
- React
- Tailwind CSS
- статическая генерация
- демонстрационная форма без серверной отправки

## Запуск локально

```bash
npm install
npm run dev
```

Открыть локально:

```text
http://localhost:3000
```

## Продакшен-сборка

```bash
npm run build
```

## Сборка для GitHub Pages

Для публикации на `https://rngoblin.github.io/agro-dev/` проект собирается как статический экспорт с базовым путём `/agro-dev`:

```bash
GITHUB_PAGES=true npm run build
```

## Проверка качества

```bash
npm run lint
```

## Деплой на Vercel

1. Загрузить проект в GitHub/GitLab/Bitbucket.
2. В Vercel выбрать `Add New Project`.
3. Импортировать репозиторий.
4. Framework preset: `Next.js`.
5. Команда сборки: `npm run build`.
6. Команда установки: `npm install`.
7. Нажать `Deploy`.
8. После деплоя заменить `metadataBase` в `app/layout.tsx` на публичный URL проекта.

## Деплой на Netlify

1. Загрузить проект в GitHub/GitLab/Bitbucket.
2. В Netlify выбрать `Add new site` → `Import an existing project`.
3. Команда сборки: `npm run build`.
4. Директория публикации: `.next`.
5. Для полноценной поддержки Next.js использовать стандартную интеграцию Netlify для Next.js.
6. После деплоя заменить `metadataBase` в `app/layout.tsx` на публичный URL проекта.

## Страницы

- `/` — демонстрационный лендинг.
- `/case` — описание кейса: задача, подход, структура, визуальное решение, улучшения, роль исполнителя.

## Публичная демо-ссылка

```text
https://rngoblin.github.io/agro-dev/
```

## Демонстрационные данные

Проект не использует реальные финансовые показатели, клиентов, договорные условия, прайс-листы, внутренние документы или производственные детали.

Подробный список: [DEMO_DATA.md](./DEMO_DATA.md).

## Короткое описание для портфолио

См. [PORTFOLIO_CASE.md](./PORTFOLIO_CASE.md).
